<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Students Add</title>
        <link rel="stylesheet" href="/css/styles.css" type="text/css" />
        <link rel="icon" href="/images/college.svg" type="image/svg+xml">
    </head>

    <body>
        <p style="font-size: 20px;"><a href="/">Home</a></p>

        <h1>Add Student</h1>

        <% if (idExists) { %> <!-- If Student ID exist in the DB -->
            <p style="color: red; font-weight: bold;">The Student ID already exists in the database. Please use a different ID.</p>
        <% } %>

        <ul>
            <li>Student ID should be 4 characters</li>
            <li>Student Name should be at least 2 characters</li>
            <li>Student age should be at least 18</li>
        </ul>

        <form action="/students/add" method="POST">
            <label for="sid">Student ID:</label>
            <input type="text" id="sid" name="sid" minlength="4" maxlength="4" required>
            <br>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" minlength="2" required>
            <br>
            <label for="age">Age:</label>
            <input type="number" id="age" name="age" min="18" required>
            <br>
            <button type="submit">Add</button>
        </form>
    </body>
</html>